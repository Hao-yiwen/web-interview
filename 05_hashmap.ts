// ============================================
// 哈希表
// ============================================

// ------------------------------------------
// 1. 赎金信 (Ransom Note) [简单]
// ------------------------------------------
// 给你两个字符串：ransomNote 和 magazine，判断 ransomNote 能不能由 magazine 里面的字符构成。
// magazine 中的每个字符只能在 ransomNote 中使用一次。
//
// 示例 1：
//   输入：ransomNote = "a", magazine = "b"
//   输出：false
//
// 示例 2：
//   输入：ransomNote = "aa", magazine = "ab"
//   输出：false
//
// 示例 3：
//   输入：ransomNote = "aa", magazine = "aab"
//   输出：true

function canConstruct(ransomNote: string, magazine: string): boolean {
  return false;
}

// ------------------------------------------
// 2. 同构字符串 (Isomorphic Strings) [简单]
// ------------------------------------------
// 给定两个字符串 s 和 t，判断它们是否是同构的。
// 如果 s 中的字符可以按某种映射关系替换得到 t，那么这两个字符串是同构的。
// 每个出现的字符都应当映射到另一个字符，同时不改变字符的顺序。
// 不同字符不能映射到同一个字符上，相同字符只能映射到同一个字符上，字符可以映射到自己本身。
//
// 示例 1：
//   输入：s = "egg", t = "add"
//   输出：true
//
// 示例 2：
//   输入：s = "foo", t = "bar"
//   输出：false
//
// 示例 3：
//   输入：s = "paper", t = "title"
//   输出：true

function isIsomorphic(s: string, t: string): boolean {
  return false;
}

// ------------------------------------------
// 3. 单词规律 (Word Pattern) [简单]
// ------------------------------------------
// 给定一种规律 pattern 和一个字符串 s，判断 s 是否遵循相同的规律。
// 这里的 遵循 指完全匹配，例如 pattern 里的每个字母和字符串 s 中的每个非空单词之间存在着双向连接的对应规律。
//
// 示例 1：
//   输入：pattern = "abba", s = "dog cat cat dog"
//   输出：true
//
// 示例 2：
//   输入：pattern = "abba", s = "dog cat cat fish"
//   输出：false
//
// 示例 3：
//   输入：pattern = "aaaa", s = "dog cat cat dog"
//   输出：false

function wordPattern(pattern: string, s: string): boolean {
  return false;
}

// ------------------------------------------
// 4. 有效的字母异位词 (Valid Anagram) [简单]
// ------------------------------------------
// 给定两个字符串 s 和 t，编写一个函数来判断 t 是否是 s 的字母异位词。
// 若 s 和 t 中每个字符出现的次数都相同，则称 s 和 t 互为字母异位词。
//
// 示例 1：
//   输入：s = "anagram", t = "nagaram"
//   输出：true
//
// 示例 2：
//   输入：s = "rat", t = "car"
//   输出：false

function isAnagram(s: string, t: string): boolean {
  return false;
}

// ------------------------------------------
// 5. 字母异位词分组 (Group Anagrams) [中等]
// ------------------------------------------
// 给你一个字符串数组，请你将 字母异位词 组合在一起。可以按任意顺序返回结果列表。
// 字母异位词是由重新排列源单词的所有字母得到的一个新单词。
//
// 示例 1：
//   输入：strs = ["eat","tea","tan","ate","nat","bat"]
//   输出：[["bat"],["nat","tan"],["ate","eat","tea"]]
//
// 示例 2：
//   输入：strs = [""]
//   输出：[[""]]
//
// 示例 3：
//   输入：strs = ["a"]
//   输出：[["a"]]

function groupAnagrams(strs: string[]): string[][] {
  return [];
}

// ------------------------------------------
// 6. 两数之和 (Two Sum) [简单]
// ------------------------------------------
// 给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出 和为目标值 target
// 的那 两个 整数，并返回它们的数组下标。
// 你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。
//
// 示例 1：
//   输入：nums = [2,7,11,15], target = 9
//   输出：[0,1]（nums[0] + nums[1] == 9）
//
// 示例 2：
//   输入：nums = [3,2,4], target = 6
//   输出：[1,2]
//
// 示例 3：
//   输入：nums = [3,3], target = 6
//   输出：[0,1]

function twoSum(nums: number[], target: number): number[] {
  return [];
}

// ------------------------------------------
// 7. 快乐数 (Happy Number) [简单]
// ------------------------------------------
// 编写一个算法来判断一个数 n 是不是快乐数。
// 「快乐数」定义为：
// - 对于一个正整数，每一次将该数替换为它每个位置上的数字的平方和。
// - 然后重复这个过程直到这个数变为 1，也可能是 无限循环 但始终变不到 1。
// - 如果这个过程 结果为 1，那么这个数就是快乐数。
//
// 示例 1：
//   输入：n = 19
//   输出：true（1² + 9² = 82 → 8² + 2² = 68 → 6² + 8² = 100 → 1² + 0² + 0² = 1）
//
// 示例 2：
//   输入：n = 2
//   输出：false

function isHappy(n: number): boolean {
  return false;
}

// ------------------------------------------
// 8. 存在重复元素 II (Contains Duplicate II) [简单]
// ------------------------------------------
// 给你一个整数数组 nums 和一个整数 k，判断数组中是否存在两个 不同的索引 i 和 j，
// 满足 nums[i] == nums[j] 且 abs(i - j) <= k。如果存在，返回 true；否则，返回 false。
//
// 示例 1：
//   输入：nums = [1,2,3,1], k = 3
//   输出：true
//
// 示例 2：
//   输入：nums = [1,0,1,1], k = 1
//   输出：true
//
// 示例 3：
//   输入：nums = [1,2,3,1,2,3], k = 2
//   输出：false

function containsNearbyDuplicate(nums: number[], k: number): boolean {
  return false;
}

// ------------------------------------------
// 9. 最长连续序列 (Longest Consecutive Sequence) [中等]
// ------------------------------------------
// 给定一个未排序的整数数组 nums，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。
// 请你设计并实现时间复杂度为 O(n) 的算法解决此问题。
//
// 示例 1：
//   输入：nums = [100,4,200,1,3,2]
//   输出：4（最长数字连续序列是 [1, 2, 3, 4]，长度为 4）
//
// 示例 2：
//   输入：nums = [0,3,7,2,5,8,4,6,0,1]
//   输出：9

function longestConsecutive(nums: number[]): number {
  return 0;
}
